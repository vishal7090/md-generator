from __future__ import annotations

# Optional: sentence-transformers grouping (Phase 3+). Import only when mdengine[log-semantic] installed.


def semantic_embeddings(_texts: list[str]) -> None:
    raise NotImplementedError("Install mdengine[log-semantic] and implement semantic_grouping")
