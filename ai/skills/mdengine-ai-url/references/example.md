# Examples — URL → Markdown

```bash
pip install "mdengine[url]"
md-url https://example.com/page ./page-bundle --artifact-layout
```

```bash
pip install "mdengine[url-full]"
# post-converts linked PDFs/office inside fetched pages when configured
```
